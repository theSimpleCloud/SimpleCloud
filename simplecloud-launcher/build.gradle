buildscript {
    dependencies {
        classpath group: 'org.reflections', name: 'reflections', version: '0.9.10'
        classpath group: 'com.google.code.gson', name: 'gson', version: '2.8.5'
    }
}

plugins {
    id 'java'
    id 'org.jetbrains.kotlin.jvm' version '1.3.50'
}

group 'eu.thesimplecloud'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8"
    compile group: 'org.reflections', name: 'reflections', version: '0.9.10'
    compile group: 'com.google.code.gson', name: 'gson', version: '2.8.5'
    compile group: 'de.flapdoodle.embed', name: 'de.flapdoodle.embed.mongo', version: '2.1.2'
    compile group: 'org.mongodb', name: 'mongo-java-driver', version: '3.11.0'
}

task fatJar(type: Jar) {
    manifest {
        attributes 'Main-Class': 'eu.thesimplecloud.launcher.LauncherMainKt'
    }
    baseName = project.name + '-all'
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}

compileKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
compileTestKotlin {
    kotlinOptions {
        jvmTarget = "1.8"
    }
}
